services:
  tiny-links:
    build: .
    container_name: tiny-links
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      APP_PASSWORD: ${APP_PASSWORD}
      SECRET_KEY: "change-this-to-a-long-random-string"
    volumes:
      - ./data:/data
